# コンポーネント仕様書: [コンポーネント名]

> **レイヤー**: Application Layer (Engine)
> **ヘッダ**: `engine/[domain]/[component_name].hpp`
> **作成日**: YYYY-MM-DD
> **最終更新**: YYYY-MM-DD

---

## 1. 概要

### 1.1 目的

<!-- このコンポーネントが解決する問題と、エンジン内での役割を記述する -->

### 1.2 分類

| 項目 | 値 |
|------|-----|
| ドメイン | `core` / `physics` / `render` / `ui` / `animation` / `text` / `shader` / `net` / `resource` |
| 満たす concept | `TaskLike` / `BehaviourLike` / `GameObjectLike` / `Drawable` / その他 |
| ThreadingPolicy | `MainThread` / `AnyThread` / `Parallel` |
| 依存コンポーネント | (例: `Transform2D`, `Vec2f`) |

---

## 2. インターフェース仕様

### 2.1 公開メンバ (プロパティ)

<!-- GUI 設定可能なプロパティを列挙する -->

| メンバ名 | 型 | デフォルト値 | 説明 |
|----------|------|-------------|------|
| `example_property` | `float` | `1.0f` | 説明 |

### 2.2 ライフサイクル

<!-- 満たす concept に応じたライフサイクルメソッドを定義する -->

| メソッド | シグネチャ | 説明 |
|---------|----------|------|
| `start()` | `void start()` | 初期化処理 |
| `update(dt)` | `void update(float dt)` | フレーム更新 |
| `release()` | `void release()` | リソース解放 |
| `draw(ctx)` | `void draw(RenderContext& ctx)` | 描画 (Drawable の場合) |

### 2.3 公開 API

<!-- ライフサイクル以外のパブリックメソッドを記述する -->

| メソッド | シグネチャ | 説明 |
|---------|----------|------|
| | | |

### 2.4 コールバック / イベント

<!-- std::function で外部に通知するイベントを列挙する -->

| コールバック名 | シグネチャ | 発火条件 |
|---------------|----------|---------|
| | | |

---

## 3. 内部設計

### 3.1 内部状態

| メンバ名 | 型 | 説明 |
|----------|------|------|
| | | |

### 3.2 アルゴリズム / 処理フロー

<!-- update() 内の主要な処理フローを記述する -->

```
1. [ステップ1]
2. [ステップ2]
3. [ステップ3]
```

### 3.3 状態遷移 (該当する場合)

<!-- StateMachine を使用する場合、状態と遷移条件を記述する -->

---

## 4. 制約・前提条件

- <!-- 例: owner_transform が非 null であること -->
- <!-- 例: start() が update() より先に呼ばれること -->

---

## 5. 実装チェックリスト

- [ ] ヘッダファイル (`engine/[domain]/[name].hpp`)
- [ ] 実装ファイル (`engine/[domain]/[name].cpp`) — ヘッダオンリーの場合は不要
- [ ] concept 準拠の確認 (`static_assert` または テストで検証)
- [ ] 単体テスト (`tests/test_[domain].cpp` に追加)
- [ ] エディタ対応 (下記セクション参照)
- [ ] CMakeLists.txt への追加 (`engine/CMakeLists.txt`)

---

## 6. エディタ対応

### 6.1 エディタ実装の有無

| 項目 | 状態 |
|------|------|
| エディタ対応 | `あり` / `なし` / `将来対応` |
| ComponentView 表示 | `あり` / `なし` |
| プロパティ編集 | `あり` / `なし` |

### 6.2 エディタ公開プロパティ

<!-- ComponentView のインスペクタに表示するプロパティを定義する -->

| プロパティ名 | 表示名 | エディタ型 | 備考 |
|-------------|--------|-----------|------|
| `example_property` | "Example" | `float (slider)` | 範囲: 0.0 - 100.0 |

### 6.3 エディタ固有の動作

<!-- プレビュー、ギズモ描画、ドラッグ操作など、エディタでの特別な挙動を記述する -->

- <!-- 例: SceneView 上にギズモを表示する -->
- <!-- 例: プロパティ変更時にリアルタイムプレビューを更新する -->

### 6.4 ネイティブ連携

<!-- Ergo.Editor.Native を介したインタロップが必要な場合に記述する -->

| C API 関数 | シグネチャ | 説明 |
|-----------|----------|------|
| | | |

---

## 7. 使用例

```cpp
// 基本的な使用例を記述する
```

---

## 8. 変更履歴

| 日付 | 変更内容 |
|------|---------|
| YYYY-MM-DD | 初版作成 |
